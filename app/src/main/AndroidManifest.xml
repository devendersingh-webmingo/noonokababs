<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.webmingo.noonokababs">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoonOKababs"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".Activity.Authentication.PrivacyPolicyActivity"
            android:theme="@style/Theme.NoonOKababs.NoActionBar" />
        <activity
            android:name=".Activity.StripePaymentGetway"
            android:theme="@style/Theme.NoonOKababs.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activity.Authentication.ProfileInfoActivity"
            android:theme="@style/Theme.NoonOKababs.NoActionBar" />
        <activity
            android:name=".Activity.Authentication.WelcomeActivity"
            android:theme="@style/Theme.NoonOKababs.NoActionBar" />
        <activity
            android:name=".Activity.Authentication.LoginActivity"
            android:theme="@style/Theme.NoonOKababs.NoActionBar" />
        <activity
            android:name=".Activity.Authentication.Succes"
            android:theme="@style/Theme.NoonOKababs.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.NoonOKababs.NoActionBar" />
        <activity
            android:name=".Activity.Authentication.VerifiyOTPActivity"
            android:theme="@style/Theme.NoonOKababs.NoActionBar" />
        <activity
            android:name=".Activity.SpleshScreen"
            android:exported="true"
            android:theme="@style/Theme.NoonOKababs.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>